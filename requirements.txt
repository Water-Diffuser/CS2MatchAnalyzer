# Runtime dependencies. Deliberately short: every one of these ships inside the
# binary, so each addition is a download the user pays for.
#
# opencv-python-headless, not opencv-python: this is a CLI, and the GUI build
# drags in GTK/Qt system libraries that are absent on a clean machine and turn
# a working binary into an import error at startup.
numpy>=1.24
opencv-python-headless>=4.8
PyYAML>=6.0
